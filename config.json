{
    "name": "QR Code",
    "tt_name": "module_name",
    "namespace": "DE\\RUB\\QRCodeExternalModule",
    "description": "A REDCap external module providing an action tag that will generate a QR code when a form is saved.",
    "tt_description": "module_desc",
    "documentation": "README.md",
    "tt_documentation": "module_docs",
    "authors": [
        {
            "name": "Günther Rezniczek",
            "email": "guenther.rezniczek@rub.de",
            "institution": "Ruhr-Universität Bochum"
        }
    ],
    "framework-version": 11,
    "permissions": [
        "redcap_save_record",
        "redcap_data_entry_form_top",
        "redcap_survey_page_top"
    ],
    "actiontags": [
        {
            "tag": "@QRCODE",
            "description": "Encodes a string value in a QR code. Apply this action tag to a field of type <b>Text Box</b> (without validation) or <b>File Upload</b> only, and specifiy the name of the source field (which needs to be on the same form) as action tag parameter: @QRCODE=\"source_field\".<br>The value of the source field will be converted to a QR code and stored in the field with the action tag. The storage format will depend on the field type:<ul><li>PNG file (stored in edocs when used on a file upload field</li><li>Base64-encoded string when used on a textbox field. This could then be used to produce inline-images with &lt;img src=\"data:image/png;base64, [field_with_qrcode]\"></li></ul>."
        }
    ],
    "system-settings": [
    ],
    "project-settings": [
    ],
    "compatibility": {
        "php-version-min": "7.4.0",
        "php-version-max": "",
        "redcap-version-min": "13.1.0",
        "redcap-version-max": ""
     }
}